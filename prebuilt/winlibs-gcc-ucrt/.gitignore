# prebuilt/winlibs-gcc-ucrt/.gitignore

# Reassembled archive — produced locally by reassemble.sh, never committed
vendor/*.7z
vendor/*.zip

# Extracted toolchain — produced locally by install.sh, never committed
toolchain/

# Split parts ARE committed (tracked in git, verified by manifest SHA256)
# vendor/*.part-aa, *.part-ab, *.part-ac — do NOT add these to .gitignore