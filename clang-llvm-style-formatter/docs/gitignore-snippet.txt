# ── clang-llvm-style-formatter ──────────────────────────────────────────────
# Per-developer hook config overrides (not shared with the team unless desired)
.llvm-hooks-local/

# clang-format / clang-tidy symlinks are committed (not ignored).
# Add the following ONLY if you copy (not symlink) the config files
# and want to manage them independently:
# .clang-format
# .clang-tidy
# ─────────────────────────────────────────────────────────────────────────────
